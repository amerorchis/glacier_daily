# Machine-local configuration (gitignored, set up once per machine).
# Copy to config.env and adjust values as needed:
#   cp config.template.env config.env
#
# All values below are the defaults used when a variable is not set.

ENVIRONMENT="development"           # "development" or "production"
FTP_SERVER="ftp.glacier.org"
MAPBOX_ACCOUNT="mapbox"
MAPBOX_STYLE="satellite-streets-v12"
DRIP_CAMPAIGN_ID="169298893"
